meta {
  name: Analyser un repas (photo)
  type: http
  seq: 2
}

post {
  url: http://localhost:8001/api/nutrition/analyze
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: COLLER_LE_TOKEN_JWT_ICI
}

body:multipart-form {
  image: @file()
}